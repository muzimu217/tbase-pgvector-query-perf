# 4. 方法学

采用“源码审计 -> 机制证据 -> 重复实验 -> 正确性核验”的顺序。主比较使用五次 ABBA 配对，报告工作负载均值、每查询均值、QPS、临时块和配对方向；正确性使用 exact 与 IVFFlat top-10 逐行排序比较。生命周期问题单独使用 1,000/10,000/50,000 rescans 的五次消融，不将其执行时间混入 SIMD 主结论。

图表脚本必须直接读取归档 CSV/JSON。每张图在图注中注明 run id、输入文件和是否为主结论。逐查询 P95/P99 未采集，不能从均值推导。

