# 6. 实验设计

主 workload 为 100,000 行、128 维、200 条固定 Cosine 查询，`lists=probes=1000`。比较 scalar 与恢复自动向量化后的构建，五次 ABBA 配对。辅助矩阵覆盖 query sort memory 与 rescan 次数；回归覆盖 L2/IP/Cosine 和 iterative scan。完整命令见 `artifacts/experiment/ivfflat-query-path-v1/commands/`。

